import React from "react";
import { Box } from "@mui/system";

interface IForgotPassword {}

const Login: React.FC<IForgotPassword> = (props) => {
  return (
    <Box>
      <p>Forgot password</p>
    </Box>
  );
};

export default Login;
